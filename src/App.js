import './App.css';

function App() {

  // Check if the task is empty or contains spaces
  const isEmpty = id => {
    if (!tasks.text || /^\s*$/.test(tasks.text)) {
      return;
    }
  }

// let updateTask

  return (
    <div className="App">
      
    </div>
  );
}

export default App;

